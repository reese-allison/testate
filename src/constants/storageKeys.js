/**
 * Centralized localStorage key constants
 * Using a single source of truth prevents typos and makes keys easier to manage
 */

export const STORAGE_KEYS = {
  FORM_DATA: 'willGenerator_formData',
  DARK_MODE: 'willGenerator_darkMode',
}
